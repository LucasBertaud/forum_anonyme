{
  "name": "forum_anonyme",
  "version": "1.5.0",
  "scripts": {
    "release:patch": "release-it patch --ci --changelog",
    "release:minor": "release-it minor --ci --changelog",
    "release:major": "release-it major --ci --changelog",
    "test:e2e": "npx playwright test --config=e2e/playwright.config.ts"
  },
  "devDependencies": {
    "playwright": "^1.52.0",
    "release-it": "^18.1.2",
    "wait-on": "^8.0.3"
  },
  "release-it": {
    "npm": {
      "publish": false
    },
    "git": {
      "requireCleanWorkingDir": false
    }
  },
  "dependencies": {
    "@nestjs/typeorm": "^11.0.0",
    "mysql2": "^3.14.1",
    "typeorm": "^0.3.22"
  }
}
